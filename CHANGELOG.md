# Changelog

All notable changes to this repository are recorded here. The format is
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this repository
follows [semantic versioning](https://semver.org/spec/v2.0.0.html), where the
public interface is the case id namespace, the result file schema and the
divergence registry format.

## [Unreleased]

### Fixed

- The `engine/index-alignment` entry is gone from the registry rather than restated, because it is no longer true. It said that firepanda lines two operands up by position and answers with the length of the left one, and on the day that stopped being the case the entry turned into an excuse: a registered divergence is scored as a decision rather than as a difference, so the cases behind it would have gone on being waved through whatever the subject did with them. Run against a build that aligns, `top["value"] + bottom["value"]` gives the union of both indexes, filled where no label appears on both sides, and an answer longer than either operand, which is what pandas gives. The four cases about a frame or a series moved to `basics`, the one about assigning a shorter column moved to `indexing`, and all five carry ids matching their new home, so the behaviour goes on being checked where a reader looking for alignment would look for it. Two of the four are absent names, `shift` and `align`, and report as unimplemented. The other two still fail, one on a null where pandas has a NaN and one on an integer column where pandas widens to double, and both of those are the open dtype questions rather than anything about the index. The registry is 7 entries.
- The alignment cases split their frames with `head` and `tail` rather than `iloc`, and none of them has a lambda inside its expression. Neither of those is a matter of taste. `iloc` does not exist in firepanda yet, so each of those expressions was raising `AttributeError` before it reached any arithmetic, which means the versions in the registry could not have told alignment from absence and were reporting on something they had never run. An inner lambda puts an absent name one frame below the depth the unimplemented rule allows, so `align`, which nobody has written, was scored as a bug rather than as a gap until the lambda came out. That is the same trap the inplace cases were in, and being able to recognise it on sight is most of the value in writing it down twice.

- An L4 case is satisfied by a subclass of the exception the case declares, on the subject side only. The rule was that the type had to match exactly on both sides, and its stated reason was that the type is what a user catches. That reason argues for the change rather than against it: a user who wrote `except KeyError` around a column lookup catches a subclass of `KeyError` without noticing, so firepanda raising `ColumnNotFoundError` there has broken nobody, and failing it for carrying the column name in its class was failing it for being more informative than the thing it copies. pandas makes the same call about itself, since its own 46 error types are subclasses of the builtins and `IntCastingNaNError` is a `ValueError` for exactly this reason. The half of the old rule that was doing real work is kept: a superclass is still a failure, because if pandas raises `MergeError` and the subject raises a plain `ValueError` then every `except MergeError` in the user's code stops firing. The pandas side stays exact, because there the question is not what a user catches but whether the case is right about pandas, and a case declaring `ValueError` where pandas raises `MergeError` states less than it knows. This raises firepanda's score by 3 cases, which is a reason to read it twice; what makes it a fix is that the rule now says what its own justification always said, and the message substring is untouched so a subclass still has to fail for the same reason and say so in the same words.

- An answer belonging to an engine that is not pandas is read rather than called a scalar. `normalize` had a branch for every pandas type, every pyarrow type and a numpy array, and then a last line wrapping anything else as a scalar, which was correct for exactly as long as the only subject was a driver handing back Arrow IPC bytes down a pipe. The module form returns the subject's own objects, so `df.head()` came back with the right rows in it and was compared against a pandas frame as though it were a number. That was 41 of the 167 failures in the first module form run and not one of them was a bug in firepanda. Anything offering `__arrow_c_stream__` or `__arrow_c_array__` is now read through that interface, after the pandas, pyarrow and numpy branches so that a pandas object keeps taking the route that knows about its labels. It is a protocol and not a type check on purpose: this module must not be able to name a type belonging to one engine, or every later argument about a comparison rule has a thumb on it, and Polars, cuDF and an engine nobody has thought of yet now arrive without a branch carrying their name.
- The row labels of a foreign frame travel into the comparison. A frame's Arrow export is its data columns and the labels are a separate object reachable through `.index`, so a conversion that read only the stream would have compared two frames while ignoring the one thing six pull requests of index work exist to get right. Every index bug in the subject would have gone quiet on the day this landed and the score would have gone up, which is the worst pair of outcomes on offer. Whether the labels are a default range is decided by reading them rather than by asking whether they are a `RangeIndex`, which cannot be asked of another library, and that is stricter in the direction that matters since `drop_index` needs both sides to agree the index is not data. The tests that hold this are the ones where the labels are deliberately wrong and the name is deliberately wrong, because a correct frame passes under the broken version too once it is a frame at all.
- `Engine.shape_of` exists, because there is exactly one thing the Arrow interface cannot say. A series and an index both cross it as a single array carrying a name, and which one it is, is a fact about the subject's type system rather than about its data. The engine answers, since the engine is the only part of this package allowed to import its subject, and it answers with `getattr` rather than a top level import so that a firepanda predating one of the three types reports None instead of raising. pandas answers None to everything and nothing about its route changes.
- The numbers after all of that, against the same firepanda 0.6.45 and pandas 3.0.3 as the run below. Cases go from 110 pass and 167 fail to 142 and 135. L3 goes from 0 to 1 and L2 from 0 to 1, and the first pandas callable firepanda has ever reached L3 on is `DataFrame.tail`. 9 of the 41 remain and both groups of them are real: 5 are `df.columns` answering with a Python list where pandas answers with an `Index`, and 4 are `s == 1` falling back to Python's default equality and returning a bool, which is [firepanda #238](https://github.com/tamnd/firepanda/issues/238). The oracle is still 4000 of 4000 and all 19 relaxations are still load bearing, which is what says this change moved the subject's score without moving the ruler.
- The module form of the subject runs, and this is the first conformance number this repository has ever produced from it. The engine was asking firepanda for `read_arrow`, which is a real function in `firepanda.io` on the Mojo side and has never been a name on the Python module, so every case came back `unimplemented` and the run looked like a subject with nothing written rather than like a harness calling something that does not exist. That is the worst shape a harness bug can take, because `unimplemented` is a real outcome here and a plausible zero raises no alarm. The engine now loads a corpus frame with `corpus.load`, the same reader the pandas side uses, and hands the table over by the Arrow C data interface. One reader and one handover, so a difference in an answer cannot be a difference in the loading.
- A registered divergence over a name that does not exist is `unimplemented` and not `divergent`. The runner was consulting the registry before it checked for absence, so an entry saying firepanda refuses an operation was being satisfied by an `AttributeError` from a method nobody has written. In the first module form run that accounted for 58 of the 64 divergent results, which is exactly the excusing this repository's rules exist to refuse: the score was reading a gap as a decision. Absence is now checked first, the record does not carry the entry id, and the entry starts being measured on the day the name exists. Nothing is let off, since `unimplemented` counts against the score as hard as a failure.
- The inplace divergence cases were being built through a trampoline lambda that fixed a late binding problem they did not have, and it cost them a traceback frame each. One frame is the whole margin the `unimplemented` depth rule works in, so every one of those cases was landing one frame too deep and being scored as a deliberate divergence instead of an absent method. With the trampoline removed the divergent count over the whole suite is 8 records on 10 names, down from 64 on 63.
- The first published module form numbers, so the next run has something to be compared against. firepanda 0.6.45 against pandas 3.0.3: L3 0 of 1125, L2 0, L1 28, L0 33, with 110 passes, 167 failures, 8 divergences and 3715 unimplemented over 4000 runs of 3157 cases. The largest single group of failures is arithmetic on a series, which is `+ - * / // % **`, unary minus and the six comparisons against a scalar, and that one gap is 91 of the 167. The pandas against pandas oracle is still 4000 of 4000.

- A pandas NaN in a numpy backed float column no longer becomes an Arrow null on the way into the comparison. pyarrow converts a pandas object using pandas' own idea of missing, which for that dtype means every NaN is a null, and that is the right answer for moving data between the two libraries and the wrong one here. This suite insists elsewhere that a null is not a NaN and says so in the difference it prints, so folding the two together on the oracle side was handing a pass to a subject engine that answered with a null where pandas answered with a NaN. The rule is narrowed to numpy float dtypes. An extension dtype carries a real mask, its `pd.NA` is a null under any rule, and it is left alone. The pandas against pandas oracle is still perfect at 4000 of 4000, which is the run that would have caught this being wrong in the other direction.
- This was found by a firepanda change rather than by inspection, which is worth recording because it is the second time the instrument has been wrong in a way only a subject could reveal. firepanda started reporting NaN from float reductions with nothing to reduce, matching pandas, and four `groupby/flat-*` cases on `keys_awkward` that had been passing started failing. They had been passing because both sides were being flattened to a null, not because the two agreed.

### Changed

- `engine/implicit-index` no longer opens by calling itself the other half of alignment. That sentence was true while there were two entries and it pointed at nothing once the alignment entry went, and a registry whose reasons refer to each other by position is a registry that quietly rots every time one is retired. The rest of the reason is unchanged and stands on its own.

- A difference about index level names says "unnamed" where a level has no name, instead of printing `NoneType(None)` at the reader. That string is what `_label` renders any `None` as, and it is deliberate and stays, because a label really can be the string `None` and a comparison that cannot tell those apart is worse than one that is ugly. The fix is at the message rather than at the renderer, so the two are still distinguishable everywhere a value is being compared and only the sentence a human reads is different. The level count check also moved in front of the level names check, because the count check returns early and a frame with no index at all was reporting the names difference and then stopping, which named the level it was missing without ever saying that it was missing one.
- `stats/argsort` names `kind="stable"` instead of taking pandas' default. The default is a quicksort, which pandas documents as not stable, so where a column holds a tie the default answer is one of several pandas is allowed to give and is not a specification anything can be measured against. `keys_10` holds exactly one duplicated value in ten thousand rows, the two kinds disagree at that tie and nowhere else, and firepanda's sort is stable, so the case was failing on a detail of numpy's introsort that could change in a patch release. It was passing on `float64_no_nulls` only because that column has no ties in it, which is the other half of the same problem: the property most worth checking here was going untested on both frames. `stats/argsort-ties` is new and runs on the key column of the same frame, ten distinct values over ten thousand rows, so nearly every position in that answer is decided by the tie break rather than by a comparison.

### Added

- The grouped result asks for the indexed shape, which is the largest single move this suite has recorded. firepanda pull request #194 gives `group_by` an `as_index` flag that puts the key in the row labels and names the level after the key column, so the 19 driver entries that were producing the flat shape and failing on it now ask for the pandas one. The groupby section goes from 54 pass and 62 fail to 111 and 5. The `flat-` twins beside each of these keep asking for `as_index=False`, because they exist to compare the arithmetic with the shape out of the way and that job has not gone away.
- What the 5 remaining groupby failures are, since none of them is an index any more. 3 are the Arrow reader refusing a dictionary encoded column, which is firepanda issue #159. 2 are the two key grouping, which pandas answers with a MultiIndex and firepanda has none, so those two cases stay in the flat spelling and keep failing on the level count. That is the truthful report rather than a gap being papered over: the driver could move one of the two keys into the index slot and turn a failure into a pass, and that is exactly the lie `docs/specs/12-the-index-gap.md` was written to refuse. basics is unchanged at 155 pass and 22 fail and stats at 52 and 4, so nothing regressed, and across the three sections the failure count is 31, down from 98 when that document was written.
- The index travels into the answer. Every result this suite has produced about firepanda so far was compared against pandas with the index thrown away on the firepanda side, because firepanda had no index and the driver hardcoded an empty one, so `default_index=True` and no level names went into every `Answer` whether that was true or not. firepanda pull request #192 gives frames and series row labels, so the driver now reports what it actually has: `index_names` and `default_index` in the header, and the labels themselves written as a leading `__index__0` column when they are not the default range. `fpcompat/driver.py` reads both, treats a driver that sends no `index_names` as sending unnamed levels so that an older driver binary still parses, and raises `DriverBroken` when a driver reports a different number of names than levels, which is the one shape of this that would otherwise compare something against nothing and call it a pass.
- What that is worth, measured against the pull request branch. Basics goes from 145 pass and 32 fail to 155 and 22, so ten cases that were failing only because the index was not being reported now pass. Stats is unchanged at 52 and 4, which is expected, since almost every case there answers with a scalar and a scalar has no index. Groupby is unchanged at 54 and 62, and the reason is the more useful finding: 57 of those 62 now read "0 index levels, expected 1" where they used to read "index names (), expected ('key',)", which is the reordered check doing its job, since that level was not missing a name, it was missing entirely. Two more say "expected 2", which is the two key grouping and wants a MultiIndex. The last three are the Arrow reader refusing a dictionary encoded column and are firepanda issue #159. So the whole of this section is now one sentence: a grouped result should be indexed by its key, which is stage two, tracked in firepanda issue #191.
- Recording that this change found a real bug in firepanda and the firepanda test suite did not. When the driver started emitting the index, groupby fell from 54 pass to 14, every new failure reading "index NoneType(None) row 0: 9, expected 0". `group_by` ends with a filter and a sort, both of which now carry labels, so the permutation of the group ordinals was leaking out as row labels where pandas has a fresh range. All 47 firepanda test files passed with that bug present. It is fixed in the same pull request and it is the plainest argument yet for running a conformance suite next to a unit suite rather than instead of asking for one.
- The eighth divergence, and the first one where firepanda is nearer the truth than pandas is rather than deliberately doing less. `engine/moment-precision`, asserted by two cases. The variance, the standard deviation, the standard error and the skewness are all computed from deviations around a mean, and a mean is a sum divided by a count, so it is near the true mean and is not it. pandas leaves that error in and firepanda now corrects for it, which came out of chasing the one `stats/skew` failure that was neither a semantics decision nor a dtype question. On ordinary columns the two agree to the last bit. On five values at two to the fifty second, where every input is exactly representable and the rounded centre is the only thing either engine can get wrong, pandas gives a variance of 37.25 where the true value is 37.2 and a skewness of 1.4863469519931585 where the true value is 1.3253147098134046, which is twelve percent. Both cases build that column themselves rather than riding on a corpus frame, on both sides, because no corpus frame is shaped to trigger it and a divergence entry has to be true every time it runs or the registry fails the build. Worth recording that this entry was added after the change rather than before it, which the rule at the top of `divergences.toml` says should not happen: the change looked like a precision fix that could only move firepanda towards pandas, and the inputs where it moves firepanda past pandas were found by measuring afterwards.
- The measurement behind that entry, which is the more useful half. On the suite's own corpus, `int64_half_null` has a true skewness of 0.12056246966568848 by 80 digit decimal arithmetic. pandas answers 5.9e-6 away from it. firepanda answered 4.3e-6 away before firepanda pull request #177 and answers 1.2e-16 away after it. On `int64_no_nulls` pandas is 3.8e-7 out, firepanda was 5.5e-6 out and is now 1.4e-7 out. So on the corpus the correction moves firepanda towards pandas and past it, and the `stats/skew` case still fails on those two frames, from the other side, because the two engines are now 2.4e-7 apart and the tolerance class is 1e-7. The section total is unchanged at 36 pass, 19 fail, 51 unimplemented, and this is the clearest case yet that a failing conformance case is a question rather than a verdict.

- The stats section in the driver, 16 cases, and the cleanest result this suite has produced about firepanda so far. Almost every case in this section answers with a scalar, and a scalar does not go through an index, so this is the first section where firepanda's arithmetic is compared to pandas without issue #154 standing in front of every result. 36 pass, 19 fail, 51 unimplemented over 106 runs, and the 19 failures are not 19 problems. Thirteen of them are firepanda issue #170 wearing two hats. Eight are a reduction with nothing left to reduce coming back as a null where pandas gives NaN, which is `std`, `var`, `sem` and `skew` over `float64_all_null`, the single row standard deviation, the median and the quantile of an all null column, and a correlation with no complete pairs. Five are the other half of the same question: `float64_no_nulls` holds one NaN and two infinities despite its name and `float64_half_null` holds 33 NaNs on top of its 32 nulls, pandas drops a NaN before it sorts and firepanda sorts it as a value, so two medians, two quantiles and `hasnans` disagree. Neither half is a bug, both are the decision in #170, and this section is the first place the cost of that decision has a number on it. Two more failures are the Arrow reader refusing a dictionary encoded column, which is #159. Two are `Series.argsort` coming back as uint32 where pandas gives int64. The last one is `skew` on the two int64 frames, and it is the interesting one.
- `sem` and `skew` in the driver, and with them the first case where this repository asked for a reduction, firepanda did not have it, it was written, and the number moved. The flat groupby family was 44 pass and 4 unimplemented; it is 48 pass and 0 unimplemented. The four were not disagreements about an answer, they were absences, and they are the whole reason to run a conformance suite against a library still being built. firepanda pull request #175 has the kernels. The section total goes from 50 pass, 58 fail, 65 unimplemented to 54 pass, 62 fail, 57 unimplemented: the four new passes are the flat forms and the four new failures are the indexed forms, which now produce an answer where they used to produce nothing and fail it on the index shape like the other 55. Of the 62 failures, 59 are firepanda issue #154 and the remaining 3 are one thing, the Arrow IPC reader refusing a dictionary encoded column, which is every `observed` case on the two categorical frames.
- The groupby section in the driver, and with it the first evidence that firepanda's grouped arithmetic is right. Running the section as it stood produced 58 failures of which 55 said only "index names (), expected ('key',)", and that is all they could ever say, because the comparison returns as soon as the two sides have different numbers of index levels and never reaches a value. A section that cannot tell a correct grouped sum from a wrong one is not measuring the thing its name says. So there is now a `flat-` case beside each aggregation, asking pandas for the same reduction with `as_index=False`, which is the shape firepanda produces, and firepanda passes 44 of those 48 runs. The four that do not are `sem` and `skew`, which firepanda does not have. Sum, mean, min, max, count, first, last, median, nunique, std and var all agree with pandas exactly across four key frames including the awkward one with a null key, an empty string key and two keys differing only by case, and so do the null keeping and unsorted forms and the two key grouping. The section total is 50 pass, 58 fail, 65 unimplemented, and every one of the 55 shape failures is firepanda issue #154 and nothing else.
- Twenty six more cases in the firepanda driver, two more answer kinds in the protocol, and the findings that came out of them. The basics section went from 55 pass, 18 fail and 341 unimplemented to 120 pass, 57 fail and 237 unimplemented, which is the first time the number moved because the instrument got better rather than because the library did. `index` and `tuple` join `scalar`, `frame` and `series` as answer kinds, because `df.columns` is an Index and `df.shape` is a tuple and neither is a Series, so folding them into one would score a real difference as a pass. The new failures sort into four piles and none of them is a surprise: sixteen are operations whose answer keeps a non default index, which firepanda has no index to keep, eight are the integer dtype question in firepanda issue #171, most of the rest strengthen the NaN as missing question in firepanda issue #170, and one is a real bug, `head(-2)` returning nothing where pandas returns all but the last two rows, now fixed in firepanda pull request #174.
- `drivers/firepanda/build.sh` takes the library checkout and the toolchain checkout as separate arguments, defaulting to the same one. The loop this repository exists to support is to find a conformance failure, fix it on a firepanda branch, rebuild the driver and see whether the number moved, and the sane place to hold that branch is a git worktree, because the main checkout usually has somebody else's work in progress in it. A worktree has no pixi environment of its own, and installing a second copy of the same pinned toolchain to compile a branch of the same library is minutes of downloading for nothing. The stamp still takes the version, the commit and the dirty flag from the library checkout, since that is the one that decides what was measured, and notes the borrowed toolchain next to the Mojo version so a result file cannot quietly claim otherwise.
- The firepanda driver, and with it the first conformance number this repository has ever produced against something other than pandas. `drivers/firepanda/main.mojo` runs the firepanda spelling of a case and writes the answer as Arrow, `fpcompat/driver.py` reads it back into the same `Answer` the pandas side builds, and nothing downstream of `compare` can tell the two forms of the subject apart. On the basics section, 414 runs: 49 pass, 24 fail, 341 unimplemented. Every one of those 24 is a statement about firepanda and the list is in firepanda issue #8. The build is `pixi run driver`, it uses the Mojo toolchain pinned by the firepanda checkout rather than one pinned here, and it stamps the version and commit it built against so that the result file can say which firepanda produced it.
- A comparison that sorts in Arrow rather than in the interpreter, and a second clock on every result record. The oracle took 195 seconds while the per case timings in the file it wrote summed to 5.1, because the clock stopped before the comparison started and roughly 160 seconds were going into a sort that rendered every value to a string and sorted Python tuples, on six merge answers of ten million rows each. Arrow sorts the key columns now and the rendered sort remains for the dictionary encoded and nested columns Arrow refuses, with both sides of a comparison always taking the same path. The oracle is 195 seconds down to 14 and the relaxation sweep 139 down to 39. `compare_seconds` is on every record and totalled on the document, so the next time the two numbers diverge the file says so.
- The relaxation sweep, `pixi run sweep`, and the correction to the specification that came with it. `05-comparison.md` said the oracle would be run a second time with each relaxation individually disabled and the cases that then failed would be the ones needing it. Measured: 18 case and relaxation pairs, run with their relaxation off, zero failures, because pandas against pandas returns both sides in the same order and an order relaxation has nothing to absorb. Under the specified rule every declaration in the repository was unnecessary and would have been deleted. The sweep reorders the oracle's own answer instead and requires each declaration to be what makes the case pass against it. All 18 declarations are load bearing.
- The specification, 12 documents in `docs/specs/`, mirrored from the author's notes.
- The scaffold: pixi environment pinned to pandas 3.0 and pyarrow 25, CI, licence.
- The surface tool, which counts the pandas API rather than remembering it, and the committed inventory of pandas 3.0.3 that every coverage number is computed against.
- The corpus, 56 frames generated from one seed, described by a committed manifest and never committed as data.
- The comparison layer, which decides what the same answer means, with four tolerance classes and a closed list of relaxations that each have to say why.
- The case registry and the runner, 623 cases over 1429 runs, four outcomes and no skip, one engine per process, and a worker crash attributed to the case that caused it.
- The surface sweep: 1413 generated L0 cases and 1034 generated L1 cases, one per name and one per readable signature, so that the two cheapest levels are measured over the whole of pandas rather than over the names somebody thought to check.
- The scoreboard, which reports against the pandas surface rather than against our own case list, prints the parameter coverage next to the score so that neither number can be quoted alone, and refuses a pull request that lowers a section without editing the recorded floor.
- The divergence registry, where a registered divergence has to actually diverge: 7 entries, 68 cases asserting them, a generated public page, and a build that fails when an entry stops being true.
- The cost matrix, 65 operations of which 21 are chains, one process per engine per operation, seven repeats with the median and the interquartile range, and a per machine baseline with a ten percent regression gate. It runs on its own corpus, which is the correctness generator and seed at one million rows.
- `operations.json`, the cost matrix operation table with the numbers taken out, committed and checked in CI so that firepanda-bench can link a query to the operations it is made of without depending on this package.
- Eleven cost matrix rows that came from firepanda-bench rather than from the parity sections, because linking each benchmark query to its operations found eleven that a published query runs and the matrix had no row for. `GroupBy.median` is the one that mattered: it keeps its values per group, and until it existed the matrix measured no reduction with per group memory in it at all.

### Fixed

- An order relaxation over a string view column raised out of pyarrow rather than reordering. Arrow's `take` has no kernel for the view layouts, so the runner wrote "the comparison itself raised" into the result file, which is the harness admitting a bug of its own in the place a reader goes for facts about the engine. firepanda's strings are views, so this was every grouped comparison on a text key rather than an exotic corner. View columns are widened to their large variants before the take, which loses nothing, since `canonical_type` already folds all three string widths into one name on the grounds that pandas has one string dtype.
- A comparison between two answers with different numbers of index levels raised `IndexError` out of pyarrow instead of reporting a difference, because the column loop is indexed by the pandas side's shape and ran off the end of the narrower one. It was found the first time a real subject produced an answer with no index, which is `DataFrame.tail` against firepanda, and until it was fixed the runner reported it as a bug in `fpcompat.compare`, which it was.
