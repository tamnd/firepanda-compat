# Divergences

Generated by `python -m fpcompat.divergences --page`. Do not edit this file, edit `fpcompat/divergences.toml` and regenerate, which CI checks.

Every entry here is a place where firepanda deliberately does not do what pandas does. None of them is a bug and none of them is a gap in the schedule, both of which are counted elsewhere. Each one is asserted by cases that run on every commit, so an entry that stopped being true fails the build rather than sitting here misleading somebody.

7 entries, asserted by 68 cases.

| entry | kind | what happens instead |
| --- | --- | --- |
| [engine/plotting](#engineplotting) | engine | the operation raises |
| [engine/pickle](#enginepickle) | engine | the operation raises |
| [engine/object-dtype](#engineobject-dtype) | engine | the operation raises |
| [engine/inplace](#engineinplace) | engine | the operation raises |
| [engine/index-alignment](#engineindex-alignment) | engine | the operation is positional. Operands of equal length are combined elementwise and operands of different lengths raise a ValueError naming both lengths, so the case that silently produces an all null answer in pandas either produces the elementwise answer or refuses. |
| [engine/implicit-index](#engineimplicit-index) | engine | there is no index unless one was declared. loc takes a declared key or raises, reindex against undeclared labels raises rather than manufacturing null rows, and reset_index has nothing to move into a column. |
| [engine/regex-lookaround](#engineregex-lookaround) | engine | the operation raises |

## engine/plotting

Plotting and styling are a different library's job. pandas ships a matplotlib front end and an HTML styler, and both of them are presentation layers built on top of a dataframe rather than parts of one. firepanda raises on every plotting and styling name with a message pointing at to_pandas(), which is a one line change for a user who wants a chart and several thousand lines the library would otherwise have to carry forever.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/plotting/*`

## engine/pickle

Reading a pickle executes whatever code is in it, so read_pickle on an untrusted file is arbitrary code execution with a friendly name. It also pins the file format to the exact class layout of the version that wrote it, which means a pickle is unreadable by any other tool and often by a later pandas. firepanda raises with a pointer to Parquet and Arrow IPC, both of which another program can read and neither of which runs anything on load.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/pickle/*`

## engine/object-dtype

A column of arbitrary Python objects is a column with no type, no memory layout and no vectorized operation available to it. Every operation on one becomes a loop that reaches into the interpreter once per element, which is where the ten to one performance difference this project exists for disappears. firepanda raises at construction, naming the column and the offending Python type, so the user finds out at the point they can still fix it rather than after their program is written.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/object-dtype/*`

## engine/inplace

The inplace parameter does not save a copy in pandas either, it only hides the assignment, and it makes every call site ambiguous about whether it returns a frame or None. It also makes a chained call silently do nothing. firepanda raises TypeError with the reassignment form in the message, so the fix is visible in the error itself. This affects 42 pandas callables and every one of them has the same one line fix.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/inplace/*`

## engine/index-alignment

Automatic alignment on the index is the largest behavioural difference in this project and the only one that changes a user's answer instead of raising. When two operands do not share labels, pandas produces the union of the labels filled with nulls, so adding two halves of one column gives an answer twice as long and entirely null, with no error anywhere. firepanda operates positionally and raises on a length mismatch. This is the entry to read twice before porting anything.

**Instead.** the operation is positional. Operands of equal length are combined elementwise and operands of different lengths raise a ValueError naming both lengths, so the case that silently produces an all null answer in pandas either produces the elementwise answer or refuses.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/alignment/*`

## engine/implicit-index

The other half of alignment. pandas gives every frame an index whether or not anybody asked for one, and then performs operations against it, so loc reads as a label lookup on numbers that are actually positions and reindex against a label nobody declared returns a row of nulls. firepanda has no implicit index, so the operations that need one either take an explicit key or work positionally, and the places where that is visible are fewer than the size of this decision suggests.

**Instead.** there is no index unless one was declared. loc takes a declared key or raises, reindex against undeclared labels raises rather than manufacturing null rows, and reset_index has nothing to move into a column.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/implicit-index/*`

## engine/regex-lookaround

firepanda's regex engine has RE2 semantics, which give a linear time guarantee in exchange for dropping backreferences and lookaround. Both of those constructs need backtracking, and backtracking is what lets one adversarial pattern take exponential time. On a hundred million rows that is not a slow query, it is a hung process. A pattern using either raises with the offending construct named, because the pattern is unsupported rather than invalid and those are different words to the person reading the message.

Kind `engine`, since 2026-09-04, specified in `06-pandas-parity.md#the-genuine-omissions`.

Asserted by:

- `divergences/regex/*`
